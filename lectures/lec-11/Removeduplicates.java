// RRemove duplicates from string

public class Removeduplicates {
    public static void remove_duplicates(String str, StringBuilder newstr, int idx, boolean map[]){
        if(idx == str.length()){
            System.out.println(newstr);
            return;
        }

        //kaam
        char currChar = str.charAt(idx);
        
    }
    public static void main(String[] args) {
        
    }
    
}
